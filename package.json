{
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "21.5.0"
  },
  "type": "module",
  "scripts": {
    "start": "node pdf_handler.js",
    "clean": "rm -rf node_modules",
    "reinstall": "npm run clean && npm install",
    "rebuild": "npm run clean && npm install && npm run build"
  }
}